import React from 'react';
import { Check, LayoutDashboard, ListTodo, Menu, Plus, Settings, Tag } from 'lucide-react';
export default function AppLayout({ children, activePage, setActivePage, onAdd }) { const links = [['Dashboard',LayoutDashboard],['My Tasks',ListTodo],['Categories',Tag]]; return <div className="shell"><aside><div className="brand"><span><Check size={17}/></span>taskflow</div><nav>{links.map(([label,Icon]) => <button className={activePage===label?'active':''} onClick={() => setActivePage(label)} key={label}><Icon size={19}/>{label}</button>)}</nav><button className="settings"><Settings size={19}/>Settings</button></aside><main><header><div><h1>{activePage === 'Dashboard' ? 'Good morning, Alex! 👋' : activePage}</h1><p>{activePage === 'Dashboard' ? 'Here’s what’s on your plate today.' : 'Organize and make progress on what matters.'}</p></div><button className="primary" onClick={onAdd}><Plus size={18}/>Add task</button></header>{children}</main></div> }
